services:
  - type: web
    name: beshbolalavash-crm-api
    runtime: node
    buildCommand: npm install
    startCommand: npm run server
    healthCheckPath: /api/health
    envVars:
      - key: JWT_SECRET
        generateValue: true